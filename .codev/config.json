{
  "shell": {
    "architect": "codex -config model_reasoning_effort=xhigh",
    "builder": "codex --config model_reasoning_effort=high",
    "shell": "bash"
  },
  "porch": {
    "checks": {
      "build": { "skip": true },
      "tests": { "command": "python3 -m unittest discover -s tests" }
    },
    "consultation": {
      "models": ["codex", "claude"]
    }
  }
}
