{
  "shell": {
    "architect": "claude --dangerously-skip-permissions",
    "builder": "claude --dangerously-skip-permissions --effort medium",
    "shell": "bash"
  },
  "porch": {
    "checks": {
      "build": { "skip": true },
      "tests": { "command": "python3 -m unittest discover -s tests" }
    }
  }
}
