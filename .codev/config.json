{
  "shell": {
    "architect": "codex -m gpt-5.5 --config model_reasoning_effort=medium",
    "builder": "codex -m gpt-5.4 --config model_reasoning_effort=xhigh",
    "shell": "bash"
  },
  "porch": {
    "checks": {
      "build": { "skip": true },
      "tests": { "command": "python3 -m unittest discover -s tests" }
    },
    "consultation": {
      "models": ["gemini", "codex", "claude"]
    }
  }
}
