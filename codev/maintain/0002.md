# Maintenance Run 0002

**Date**: 2026-07-16
**Base Commit**: 3fb1089 (finalize run file 0001, PR #24)
**PR**: #30

## Changes Since Last Run

18 commits since run 0001 (`3fb1089..HEAD`), mostly porch state. The substantive work:

- **Spec 27 (AIR, PR #28)** — added the **Antigravity CLI observer** (`agy`): new `bin/agy`
  shim, `agy` added to `OBSERVER_SHIM_NAMES` in `src/ai_observe/observe.py`, and `agy`
  threaded through the resolver / packaging-smoke / shim-resilience test matrices.
- **Spec 25 (AIR, PR #26)** — added `CHANGELOG.md` (0.1.0) and recorded a release-checklist run.
- `aeac873` — `.codev/config.json` gained `build_succeeds`/`tests_pass` check aliases to
  protect porch for this Python project against a future codev `getPhaseCompletionChecks`.

No new files under `codev/reviews/` since run 0001 (specs 25 and 27 ran AIR, which records
narrative in `codev/state/*_thread.md`, not a numbered review), so lessons-learned had no
new review harvest to mine.

## Audit Findings

Governance-doc audit run per Step 3a (update-arch-docs, audit-mode) across all four files.
Per architect guidance (2026-07-16), every code-referencing claim was **actively verified
against the current codebase** — not kept out of caution — to hunt for entries referencing
removed code, retired components, or superseded behavior. Verified live and current:

- Backend seam — `src/ai_observe/backends/` with `Backend`, `BackendCapabilities`,
  `BackendState`, `BackendSession`, `StraceBackend`, `SnapshotBackend` all present;
  `AI_OBSERVE_BACKENDS` = `strace,snapshot` default all in code.
- Generic core — `observe.py` present; `codex_observe.py` compatibility facade still aliases
  the generic module (`sys.modules[__name__] = _observe`); `CODEV_OBSERVE_*` aliases present.
- Provenance — `schema_version` / `source` / `confidence` / `direct` / `inferred` all in code.
- Artifact contract — all five sibling suffixes (`.trace`, `.jsonl`, `.jsonl.partial`,
  `.jsonl.rebuilt`, `.meta.json`) emitted by the code.
- Packaging — pyproject console scripts are exactly `ai-observe` + `ai-observe-viewer`;
  `viewer/static/*` assets and `viewer/__main__.py` present; `static/*` package-data declared.
- CI — `.github/workflows/ci.yml` matches every arch.md assertion: Python 3.10/3.12/3.13,
  `fail-fast: false`, guarded `ptrace_scope=0`, Node 20, and the exact fail-on-skip grep
  `\.\.\. skipped|skipped=[0-9]`.

Result within the **governance docs**: no stale entries → no cuts. Every documented claim
maps to live code.

**Broader doc-sync (added after PR-iter1 review — codex REQUEST_CHANGES, correctly):** the
first pass scoped "documentation" to the governance docs only and claimed `arch.md` was the
sole staleness. That was too narrow. A repo-wide sweep (`grep -rl opencode`) found the
spec-27 `agy` shim was also **omitted from every current-state shim enumeration outside the
governance docs**: `pyproject.toml`, `README.md`, and `docs/observe.md`. All fixed additively
(see What Was Done).

**Deliberate non-changes (verified NOT stale — release history, not current-state):**
- `CHANGELOG.md` `## [0.1.0]` entry lists the four pre-agy shims. `agy` shipped in spec 27
  (merge `beb59b9`) **after** the `v0.1.0` tag was cut at `77dae8b` (spec 25) — `git cat-file
  -e v0.1.0:bin/agy` fails, confirming `bin/agy` is not in the release. Retroactively adding
  `agy` to a shipped release entry would falsify history. Left as-is; it is accurate.
- `codev/plans/11-*.md` and `codev/plans/20-*.md` enumerate the older shims. Plans are
  historical artifacts fixed at their spec's time (both predate spec 27). Not edited.

### arch.md (cold) / arch-critical.md (hot)
- **arch.md**: no cuts. 9 sections, all current-state; no aspirational/changelog sections,
  no stale per-file enumerations, no meta-spec duplication. All path references resolve
  (`.github/workflows/ci.yml`, `src/`, `bin/` all present; the `codex_observe.py`
  compatibility alias referenced at the "Generic command observer core" invariant still
  exists). Two shim enumerations were **stale by omission** — they listed the four older
  named shims but not the spec-27 `agy` shim; fixed additively (not a cut). The inline
  `(Spec 20)` / `(Spec 21)` attributions are single parentheticals, not changelog sections
  — KEPT for traceability.
- **arch-critical.md**: no changes. 1 fact + 9-topic map == arch.md's 9 top-level sections
  (map accurate), 21 lines — within the ≤10-fact / ≤12-topic / ≤35-line cap.

### lessons-learned.md (cold) / lessons-critical.md (hot)
- **lessons-learned.md**: no cuts. 15 terse topical sections (1–3 sentences / one short
  paragraph each), no spec-numbered framing, no duplicate adjacent entries; all are
  reference-worthy recipes kept per "spec-narrow recipes stay in cold." No new reviews to
  harvest this run.
- **lessons-critical.md**: no changes. 4 lessons + 11-topic map, 26 lines — within cap; the
  map's 11 thematic topics still cover all 15 cold sections accurately. The spec-27 `agy`
  addition introduced no new cross-cutting engineering lesson (it reuses the existing named-
  shim pattern already documented), so nothing was promoted to the hot tier.

## What Was Done

### Dead Code Removed
- None. Python project with zero third-party runtime deps; `ts-prune`/`depcheck` are
  JS-only and `vulture`/`pyflakes` are not installed (importing `vulture` fails). Same
  posture as run 0001: the codebase is small and fully exercised by the test suite, so per
  "skipping validation is not validation," no removals were manufactured without tooling to
  prove them dead. Deferred (see below).

### Documentation Updated
Synced the spec-27 Antigravity CLI observer (`agy`) into every current-state shim
enumeration across the repo:
- **arch.md**: two named-shim enumerations — "Generic command observer core" invariant and
  "Packaging and distribution" not-an-entry-point list.
- **pyproject.toml**: `[project.scripts]` header comment listing the intentionally-not-
  installed named shims.
- **README.md**: the intro "deliberately not installed" list and the "Named shims" section's
  `bin/*` enumeration.
- **docs/observe.md**: the "Named shims" `bin/*` list, the `AI_OBSERVE_REAL_* → bin/*`
  mapping list (added `AI_OBSERVE_REAL_AGY → bin/agy`), and the environment-variable
  reference table (added the `AI_OBSERVE_REAL_AGY` row).

### Documentation Changes Log
| Document | Section | Action | Reason |
|----------|---------|--------|--------|
| arch.md | "Generic command observer core" — named-shim invariant | UPDATED | Stale by omission — added spec-27 `bin/agy` |
| arch.md | "Packaging and distribution" — not-an-entry-point shims | UPDATED | Stale by omission — added spec-27 `agy` |
| pyproject.toml | `[project.scripts]` header comment | UPDATED | Stale by omission — named-shim list omitted `agy` |
| README.md | intro "not installed" list + "Named shims" section | UPDATED | Stale by omission — current-state shim lists omitted `agy` |
| docs/observe.md | "Named shims" list, env→shim mapping, env-var table | UPDATED | Stale by omission — added `bin/agy` + `AI_OBSERVE_REAL_AGY` |
| CHANGELOG.md | `## [0.1.0]` shim list | KEPT | NOT stale — `agy` shipped after the `v0.1.0` tag; editing a released entry would falsify history |
| codev/plans/11,20 | shim enumerations | KEPT | NOT stale — historical plan artifacts predating spec 27 |

### CLAUDE.md / AGENTS.md
- In sync. `diff` shows only the intentional differences: the title line
  (`Claude Code Instructions` vs `AI Agent Instructions`) and AGENTS.md's AGENTS.md-standard
  note. Bodies (including the `@codev/resources/*-critical.md` hot-tier includes, which did
  not change this run) are otherwise identical. No action.

## Deferred

- Installing `vulture`/`pyflakes` into CI for automated dead-code auditing (re-flagged from
  run 0001). Would let future MAINTAIN runs prove unused exports with tool backing rather
  than manual grep. Not adding a dev dependency inside a maintenance run — flag for a future
  SPIR/AIR if desired.
- Recording release notes in `CHANGELOG.md` is now the project convention (spec 25). Not
  mirrored into arch.md: changelogs are git-log / spec / review territory per the routing
  matrix, and the CHANGELOG is self-documenting. Noted here for awareness only.

## Summary

Maintenance pass syncing the spec-27 `agy` (Antigravity CLI) observer into the docs. The
governance docs were already lean and current — no cold-tier cuts, both hot files within cap
with accurate maps. The substantive work was completing the `agy` doc-sync across every
current-state shim enumeration: `arch.md`, `pyproject.toml`, `README.md`, and
`docs/observe.md` (the broader scope was surfaced by the PR-iter1 codex review after an
over-narrow first pass). Release-history entries (`CHANGELOG.md` `[0.1.0]`, historical
plans) were verified NOT stale — `agy` post-dates the `v0.1.0` tag — and correctly left
untouched. No dead code removed (no tooling to prove anything dead; suite green, 236/0).
