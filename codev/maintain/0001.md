# Maintenance Run 0001

**Date**: 2026-07-14
**Base Commit**: b903aad (first maintenance run — no prior run to diff against)
**PR**: #24

## Changes Since Last Run

First MAINTAIN run for `ai-observe`. History spans specs 1–21 (350 commits): filesystem
observer core, generic command observer, layered strace+snapshot backends, provenance
model, browser viewer, packaging (spec 20), and CI/test-reliability (spec 21). Governance
docs (`arch.md`, `lessons-learned.md`) were kept current by the SPIR builders as they
shipped, so the cold tiers are already in good shape; the hot tier had unfinished setup.

## Audit Findings

### arch.md (cold) / arch-critical.md (hot)
- **arch.md**: no cuts. 9 sections, all current-state, no changelog/aspirational framing,
  no stale per-file enumerations. All `src/`, `bin/`, `.github/` path references resolve.
- **arch-critical.md**: map is accurate (9 topics == arch.md's 9 top-level sections),
  1 fact, within cap. Trimmed the obsolete `STARTER: replace the examples below…` sentence
  from the guidance comment — the examples were already replaced with real content, so the
  instruction was stale (OBSOLETE). Cap/displacement guidance retained.

### lessons-learned.md (cold) / lessons-critical.md (hot)
- **lessons-learned.md**: no cuts. 15 terse topical sections (1–3 sentences / one short
  paragraph each), no spec-numbered framing, no duplicate adjacent entries. All are
  reference-worthy recipes — kept per "spec-narrow recipes stay in cold."
- **lessons-critical.md**: primary defect of this run — the file still carried unreplaced
  STARTER placeholders. Fixed:
  - Removed placeholder lesson line `<Add your project's hard-won… one line each.>`.
  - Filled the empty `Map of lessons-learned.md` (was two `<Top-level section>` /
    `<List your … sections here>` placeholders) with an 11-topic thematic map covering all
    15 cold sections, each with a one-line "consult when…". Under the ≤12-topic cap.
  - Promoted one project behavior-changer into the hot lessons: "Stage new files the moment
    you create them — porch's commit sweep only commits staged files." This is cross-cutting
    (every future builder under porch) and behavior-changing up front; the detailed recipe
    stays in cold as "Stage new files immediately; know your orchestrator's commit sweep."
  - Trimmed the same obsolete `STARTER:` comment sentence (OBSOLETE).
  - Hot cap after edit: 4 lessons + 11 map topics, 28 lines — within cap.

## What Was Done

### Dead Code Removed
- None. No `ts-prune`/`depcheck` (Python project, zero third-party runtime deps — pyproject
  declares no `dependencies`). `vulture`/`pyflakes` not installed; the codebase is small and
  actively exercised by 236 tests. Per "when in doubt, KEEP" and "skipping validation is not
  validation," no removals were manufactured without tooling to prove them dead.

### Documentation Updated
- lessons-critical.md (hot): filled the cold-doc map, removed STARTER placeholders, promoted
  1 cross-cutting behavior-changer, trimmed stale comment. Flows into CLAUDE.md/AGENTS.md via
  their `@codev/resources/lessons-critical.md` includes.
- arch-critical.md (hot): trimmed stale STARTER comment sentence.

### Documentation Changes Log
| Document | Section | Action | Reason |
|----------|---------|--------|--------|
| lessons-critical.md | "Critical lessons" placeholder line | DELETED | OBSOLETE — unreplaced STARTER template line |
| lessons-critical.md | "Map of lessons-learned.md" | FILLED | Was STARTER placeholder; mapped to 11 thematic topics over cold doc's 15 sections |
| lessons-critical.md | "Critical lessons" | ADDED | Promoted porch commit-sweep rule (behavior-changing, cross-cutting) |
| lessons-critical.md | guidance comment | TRIMMED | OBSOLETE — `STARTER: …` instruction already satisfied |
| arch-critical.md | guidance comment | TRIMMED | OBSOLETE — `STARTER: …` instruction already satisfied |

### CLAUDE.md / AGENTS.md
- In sync. `diff` shows only the intentional differences: the title line
  (`Claude Code Instructions` vs `AI Agent Instructions`) and AGENTS.md's AGENTS.md-standard
  note. Bodies are otherwise identical. No action.

## Deferred

- Installing `vulture`/`pyflakes` into CI for automated dead-code auditing would let future
  MAINTAIN runs catch unused exports with tool backing rather than manual grep. Not adding a
  dependency in a maintenance run — flag for a future SPIR/AIR if desired.

## Summary

First maintenance pass. Cold governance tiers (`arch.md`, `lessons-learned.md`) were already
current and needed no cuts. The real work was finishing the hot tier: `lessons-critical.md`
still shipped STARTER placeholders (empty map + placeholder lesson), now filled with an
11-topic map, a promoted cross-cutting behavior-changer, and stale-comment trims in both hot
files. No dead code removed (no tooling to prove anything dead; 236 tests green, 0 skips).
